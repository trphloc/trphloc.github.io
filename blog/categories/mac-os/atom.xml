<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Mac Os | Phuoc-Loc's blog]]></title>
  <link href="http://trphloc.github.io/blog/categories/mac-os/atom.xml" rel="self"/>
  <link href="http://trphloc.github.io/"/>
  <updated>2015-03-31T14:41:44+07:00</updated>
  <id>http://trphloc.github.io/</id>
  <author>
    <name><![CDATA[Phuoc-Loc Truong]]></name>
    <email><![CDATA[trphloc@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Bugzilla]]></title>
    <link href="http://trphloc.github.io/blog/2015/03/31/bugzilla/"/>
    <updated>2015-03-31T14:04:52+07:00</updated>
    <id>http://trphloc.github.io/blog/2015/03/31/bugzilla</id>
    <content type="html"><![CDATA[<h2>Cài đặt bugzilla trên mac os</h2>

<!-- ================================================================= -->


<h2>B1: Cài đặt perl</h2>

<ul>
<li>Sử dụng ActivePerl</li>
<li>Hoặc sử dụng Perlbrew
=> Sau khi cài đặt phải tạo biến (symbolic link) của perl tới thư mục mặc định</li>
</ul>


<p>```</p>

<pre><code>sudo ln -s /usr/local/ActivePerl-5.8/bin/perl /usr/bin/perl
</code></pre>

<p>```</p>

<p>hoặc</p>

<p>```</p>

<pre><code>sudo ln -s /Users/trphloc/perl5/perlbrew/perls/perl-5.20.1/bin/perl /usr/bin/perl
</code></pre>

<p>```</p>

<!-- ================================================================= -->


<h2>B2: Cài đặt make</h2>

<ul>
<li>Thực hiện các lệnh sau (các lệnh sau, phải thực hiện trong cpan</li>
</ul>


<p>```</p>

<pre><code>cpan
o conf make '/usr/bin/make'
o conf commit
exit
</code></pre>

<p>```</p>

<ul>
<li>Tham khảo: <a href="http://askubuntu.com/questions/161060/how-do-i-install-make">http://askubuntu.com/questions/161060/how-do-i-install-make</a></li>
</ul>


<!-- ================================================================= -->


<h2>B3: Một số gói cần cài đặt (tùy chọn)</h2>

<p>```</p>

<pre><code>brew install libgd
</code></pre>

<p>```</p>

<p>```</p>

<pre><code>cpan install patchutils
cpan upgrade DBI
</code></pre>

<p>```</p>

<!-- ================================================================= -->


<h2>B4: Cài đặt Driver cho cơ sở dữ liệu</h2>

<ul>
<li>Postgres</li>
</ul>


<p>```</p>

<pre><code>sudo perl -MCPAN -e "install DBD::Pg"
</code></pre>

<p>```</p>

<ul>
<li>mysql</li>
</ul>


<p>```</p>

<pre><code>sudo -H cpan DBD::mysql
</code></pre>

<p>```</p>

<!-- ================================================================= -->


<h2>B5: Cài đặt bugzilla</h2>

<ul>
<li>Lấy mã nguồn theo đường dẫn: &ldquo;sudo git clone &mdash;branch bugzilla-4.4-stable <a href="https://git.mozilla.org/bugzilla/bugzilla">https://git.mozilla.org/bugzilla/bugzilla</a>&rdquo;</li>
<li>Di chuyển vào thư mục bugzilla</li>
<li>Thực thi lệnh</li>
</ul>


<p>```</p>

<pre><code>sudo perl ./checksetup.pl
</code></pre>

<p>```</p>

<ul>
<li>Nếu có lỗi (báo thiếu module) thì cài thêm module theo hướng dẫn hoặc có thể thực hiện lệnh sau</li>
</ul>


<p>```</p>

<pre><code>sudo perl install-module.pl --all
</code></pre>

<p>```</p>

<ul>
<li>Nếu ko có lỗi (hoặc đã cài bổ sung đủ module) thì thực thi lại lệnh checksetup, lệnh sẽ phát sinh thêm <strong>localconfig</strong> trong cùng thư mục</li>
</ul>


<p>```</p>

<pre><code>sudo perl ./checksetup.pl
</code></pre>

<p>```</p>

<ul>
<li>Vào tập tin localconfig sửa các thông tin của database</li>
</ul>


<p>```</p>

<pre><code>$db_host = 'localhost';
</code></pre>

<p>   # The name of the database. For Oracle, this is the database&rsquo;s SID. For
   # SQLite, this is a name (or path) for the DB file.
   $db_name = &lsquo;bugzilla&rsquo;;</p>

<p>   # Who we connect to the database as.
   $db_user = &lsquo;postgres&rsquo;;</p>

<p>   # Enter your database password here. It&rsquo;s normally advisable to specify
   # a password for your bugzilla database user.
   # If you use apostrophe (&lsquo;) or a backslash () in your password, you&rsquo;ll
   # need to escape it by preceding it with a &rsquo;\&lsquo; character. (\&rsquo;) or ()
   # (It is far simpler to just not use those characters.)
   $db_pass = &lsquo;&rsquo;;</p>

<p>   # Sometimes the database server is running on a non-standard port. If that&rsquo;s
   # the case for your database server, set this to the port number that your
   # database server is running on. Setting this to 0 means &ldquo;use the default
   # port for my database server.&rdquo;
   $db_port = 5432;
```</p>

<ul>
<li>Thực thi lại lệnh checksetup</li>
</ul>


<p>```</p>

<pre><code>sudo perl ./checksetup.pl
</code></pre>

<p>```</p>

<ul>
<li>Thành công thì có thể truy cập vào theo đường dẫn</li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Perl]]></title>
    <link href="http://trphloc.github.io/blog/2015/03/31/perl/"/>
    <updated>2015-03-31T11:39:38+07:00</updated>
    <id>http://trphloc.github.io/blog/2015/03/31/perl</id>
    <content type="html"><![CDATA[<h1>Perl trên mac os</h1>

<!-- ================================================================= -->


<h2>ActivePerl</h2>

<h3>Cài đặt</h3>

<ul>
<li>Tải tại đây (<a href="http://www.activestate.com/activeperl/downloads">http://www.activestate.com/activeperl/downloads</a>) và cài đặt theo chỉ dẫn</li>
<li>Thêm đường dẫn vào biến PATH (sử dụng file .profile)</li>
</ul>


<p>```</p>

<pre><code>export PATH=/usr/local/ActivePerl-5.8/bin:$PATH
export PATH=/usr/local/ActivePerl-5.8/site/bin:$PATH
</code></pre>

<p>```</p>

<ul>
<li>Tạo Symbolic link tới /usr/bin/perl và ppm</li>
</ul>


<p>```</p>

<pre><code>ln -s /usr/local/ActivePerl-5.8/bin/perl /usr/bin/perl
ln -s /usr/local/ActivePerl-5.8/bin/ppm /usr/bin/ppm
</code></pre>

<p>```</p>

<h3>Xóa</h3>

<p>```</p>

<pre><code>sudo /usr/local/ActivePerl-5.8/bin/ap-uninstall
rm /Users/&lt;username&gt;/bin/perl
</code></pre>

<p>```</p>

<ul>
<li>Tham khảo:

<ul>
<li><a href="http://docs.activestate.com/activeperl/5.8/install.html#installing%20activeperl%20on%20mac%20os%20x%20(x86,%20powerpc">docs.activestate.com</a>)</li>
</ul>
</li>
</ul>


<!-- ================================================================= -->


<h2>Perlbrew</h2>

<ul>
<li>Cài đặt nhiều phiên bản perl trên máy, chọn sử dụng version phù hợp cho mỗi tác vụ</li>
<li>Cài đặt perlbrew</li>
</ul>


<p>```</p>

<pre><code>\curl -L http://install.perlbrew.pl | bash
</code></pre>

<p>```
&ndash; Cài đặt và sử dụng 1 phiên bản perl cụ thể</p>

<p>```</p>

<pre><code>perlbrew install perl-5.16.0
perlbrew switch perl-5.16.0
</code></pre>

<p>```</p>

<ul>
<li>Tham khảo:

<ul>
<li><a href="http://perlbrew.pl/">http://perlbrew.pl/</a></li>
</ul>
</li>
</ul>

]]></content>
  </entry>
  
</feed>
